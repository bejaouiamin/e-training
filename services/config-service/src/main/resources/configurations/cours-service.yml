
spring:
  datasource:
      url: jdbc:postgresql://localhost:5433/cours_db
      username: training       # ✓ Correct
      password: training       # ✓ Correct
      driver-class-name: org.postgresql.Driver
  jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: true

server:
  port: 8085
